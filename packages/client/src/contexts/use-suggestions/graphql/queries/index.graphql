query GetSuggestionsForCv($cvId: ID!) {
  getSuggestionsForCv(cvId: $cvId) {
    blockId
    comments {
      _id
      cvId
      cvVersionId
      blockId
      text
      suggestedText
      startOffset
      endOffset
      status
      authorName
      createdAt
      updatedAt
    }
  }
}
